# HawkFish Configuration Example
# Copy this file to hawkfish.env and customize as needed

# API Configuration
HF_API_HOST=0.0.0.0
HF_API_PORT=8080

# Libvirt Configuration
LIBVIRT_URI=qemu:///system

# Authentication
HF_AUTH=sessions
HF_DEV_TLS=off

# UI
HF_UI_ENABLED=true

# Storage
HF_STATE_DIR=/var/lib/hawkfish
HF_ISO_DIR=/var/lib/hawkfish/isos

# Connection Pooling
HF_LIBVIRT_POOL_MIN=1
HF_LIBVIRT_POOL_MAX=10
HF_LIBVIRT_POOL_TTL_SEC=300

# TLS Configuration (for custom mode)
# HF_TLS_CERT=/etc/hawkfish/certs/server.crt
# HF_TLS_KEY=/etc/hawkfish/certs/server.key

# Development token (for auth_mode=none)
# HF_DEV_TOKEN=dev-token-12345
